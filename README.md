# homebrew-flowmark

Homebrew tap for [flowmark](https://github.com/jlevy/flowmark-rs), a Markdown
auto-formatter for clean diffs and semantic line breaks.

## Install

```bash
brew tap jlevy/flowmark
brew install flowmark
```

## Update

```bash
brew update
brew upgrade flowmark
```
